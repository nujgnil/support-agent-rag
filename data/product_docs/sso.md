# SAML SSO

Enterprise workspaces can configure SAML SSO from Admin Settings. The service provider ACS URL and entity ID are shown in the SSO configuration screen.

After uploading identity provider metadata, test login with a non-owner account before enforcing SSO for all users.
