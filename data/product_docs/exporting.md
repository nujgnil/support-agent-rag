# Data Exporting

CSV exports support up to 100,000 rows per job. Large reports may take several minutes to complete.

If an export fails, retry with a narrower date range and confirm that the workspace has export permissions enabled. Persistent failures should include the export job ID in escalation notes.
